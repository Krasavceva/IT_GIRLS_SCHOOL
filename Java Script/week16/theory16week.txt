Неделя 16 вопросы:
1.
С помощью [] : let arr = [item, item2...];
new Array (очень редко) : let arr = newArray(item, item2...);

2.
Список товаров в интернет-магазине
Список сотрудников в компании
Список контактов клиентов из формы обратной связи

3. Nums.lenght = 0 (очищает массив)

4. Можно пропускать. Все части цикла являются не обязательными. Если написать for( ; ; ), то код будет считывать числа до тех пор, пока пользователь их водит.

5. Цикл while - для повторения кода заданное количество раз.
let i = 0;
while (i < 3) {
  alert(i);
  i++;
}
let n = 0;
let x = 0;
while (n < 3) {
  n++;
  x += n;
}

6. new Array(5) - массив получится пустой, без элементов, но с длиной 5.

7. for (let i = 2; i <= 10; i++) {
if (i % 2 == 0)
alert(i);
}

8. let arr = ["a", "b"];
arr.push(function() {
alert( arr );
})
arr[2](); // "c"

9.
Цикл for
Цикл for...of
метод forEach ()

10. let input = document.querySelectorAll(input).

11. При помощи метода sort().

12. С помощью директивы break.